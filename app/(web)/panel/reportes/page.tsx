import { PlaceholderPanel } from "@/components/web/placeholder-panel";

export default function ReportesPanelPage() {
  return (
    <PlaceholderPanel
      titulo="Reportes"
      descripcion="Acá va el consolidado de la organización, por categoría/período, con export Excel/CSV."
    />
  );
}
